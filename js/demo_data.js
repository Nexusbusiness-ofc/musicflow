/**
 * MusicFlow Data Seeder
 * Predefined tracks have been removed per user configuration.
 */

export async function seedDemoDataForUser(userId) {
  // Demo/predefined tracks have been permanently removed
  return [];
}

